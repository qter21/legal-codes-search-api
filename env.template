# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
MONGODB_DATABASE=legal_codes
MONGODB_COLLECTION=ca_codes

# Elasticsearch Configuration
ELASTICSEARCH_URL=http://localhost:9200
ELASTICSEARCH_INDEX=legal_codes

# Qdrant Configuration
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=legal_codes_vectors

# Embedding Model Configuration
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DEVICE=cpu  # cpu, cuda, or mps

# Sync Configuration
BATCH_SIZE=1000
SYNC_SCHEDULE=0 2 * * *  # Cron format: Daily at 2 AM
SYNC_MODE=incremental  # incremental or full

# API Configuration (Prototype Testing)
API_HOST=0.0.0.0
API_PORT=8888  # Prototype runs on port 8888 (independent from main website)
LOG_LEVEL=INFO

# Performance
WORKERS=4
EMBEDDING_BATCH_SIZE=32

